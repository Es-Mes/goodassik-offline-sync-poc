{
    "name": "integration-tests",
    "version": "1.0.0",
    "description": "Integration tests for bi-directional sync system",
    "scripts": {
        "test": "jest --runInBand --verbose",
        "test:watch": "jest --watch --runInBand"
    },
    "jest": {
        "testEnvironment": "node",
        "testTimeout": 30000,
        "verbose": true
    },
    "dependencies": {
        "axios": "^1.6.2",
        "better-sqlite3": "^9.2.2",
        "form-data": "^4.0.5",
        "pg": "^8.11.3",
        "uuid": "^14.0.0"
    },
    "devDependencies": {
        "jest": "^29.7.0"
    }
}
